---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: autoconfig-tls
  namespace: prod
spec:
  secretName: autoconfig-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - autoconfig.aramakisai.com
