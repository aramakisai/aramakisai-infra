---
# Wiki.js DB 日次フルバックアップ (毎日 02:00 UTC)
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: wikijs-db-daily
  namespace: prod
spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: wikijs-db
  target: prefer-standby
