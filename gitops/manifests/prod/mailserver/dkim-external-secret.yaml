---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mailserver-dkim
  namespace: prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical
  target:
    name: mailserver-dkim
    creationPolicy: Owner
  data:
    - secretKey: dkim-key
      remoteRef:
        key: MAILSERVER_DKIM_PRIVATE_KEY
