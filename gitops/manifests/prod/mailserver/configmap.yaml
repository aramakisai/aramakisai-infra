---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mailserver-config
  namespace: prod
data:
  # Dovecot passdb/userdb (FILE provisioner 時に 10-auth.conf から include される):
  # - passdb は Zitadel Session API への委譲 (lua) のみ。postfix-accounts.cf の ML
  #   アドレスは配送専用で、passwd-file passdb を置かないことでログイン不可にする。
  # - userdb は passwd-file (ML アドレス、DMS が postfix-accounts.cf から生成) を先に引き、
  #   見つからなければ lua (Zitadel ユーザー) へフォールスルーする。
  # mechanisms = plain login により、OAUTHBEARER (Roundcube) は dovecot.cf の oauth2 passdb のみを使う。
  auth-passwdfile.inc: |
    passdb {
      driver = lua
      args = file=/etc/dovecot/zitadel-auth.lua blocking=yes
      mechanisms = plain login
      result_failure = continue
      result_internalfail = continue
    }
    userdb {
      driver = passwd-file
      args = username_format=%u /etc/dovecot/userdb
    }
    userdb {
      driver = lua
      args = file=/etc/dovecot/zitadel-auth.lua blocking=yes
    }
  # 上記passdb/userdbが呼び出すLuaスクリプト本体
  # (ansible/roles/zitadel-cutover/templates/zitadel-auth.lua.j2 のレンダリング結果、
  # dovecot-lua-2.3.21.1 + lua5.3-socket + lua5.3-cjson で実機検証済み)。
  zitadel-auth.lua: |
    -- Dovecot Lua Auth Bridge: passdb/userdb driver for Zitadel Session API
    --
    -- passdb は auth_passdb_lookup (lookup方式) を使う。Zitadel Session API側で
    -- 平文パスワードの検証が完結するため、Dovecotには独自のパスワードスキーム比較を
    -- させない ("nopassword=y" を返す。付けないとDovecotがpassword=フィールドを
    -- 要求しPASSDB_RESULT_SCHEME_NOT_AVAILABLEで失敗する。dovecot-lua-2.3.21.1 +
    -- luasocket + lua5.3-cjsonの組み合わせでk3d実機検証済み)。
    --
    -- この設定はログイン主体(Zitadelユーザー)の認証のみを対象とする。ML アドレスの
    -- 配送先解決は DMS の FILE provisioner (postfix-accounts.cf) が担う。

    local http = require("socket.http")
    local ltn12 = require("ltn12")
    local cjson = require("cjson.safe")

    -- 環境ごとの値はJinja2レンダリング時に固定する(Dovecotにはenv_設定機構が
    -- 存在せず、import_environmentはDMSの既定env一覧を破壊するリスクがあるため
    -- 採用しなかった。k3d実機でこの2点を確認済み)。
    local ZITADEL_API_BASE_URL = "http://zitadel.zitadel.svc.cluster.local:8080"
    local ZITADEL_EXTERNAL_DOMAIN = "idp.aramakisai.com"
    local PAT_FILE = "/run/secrets/zitadel-dovecot-pat"
    local HTTP_TIMEOUT_SECONDS = 5

    http.TIMEOUT = HTTP_TIMEOUT_SECONDS

    local function read_file(path)
      local f = io.open(path, "r")
      if not f then
        return nil
      end
      local content = f:read("*a")
      f:close()
      if content == nil then
        return nil
      end
      return (content:gsub("%s+$", ""))
    end

    -- auth-workerプロセスは複数リクエストにわたって存続するため、ファイル読み取りは
    -- プロセス起動後1回のみで良い(PATローテーション時はauth-workerの再起動が必要)。
    local cached_pat = nil
    local function get_pat()
      if cached_pat == nil then
        cached_pat = read_file(PAT_FILE)
      end
      return cached_pat
    end

    -- 戻り値: ok(bool), status_code_or_nil, decoded_body_or_error_string
    local function zitadel_api(method, path, body_table)
      local pat = get_pat()
      if pat == nil then
        return false, nil, "pat_unavailable"
      end

      local body = nil
      local headers = {
        ["Host"] = ZITADEL_EXTERNAL_DOMAIN,
        ["Authorization"] = "Bearer " .. pat,
      }
      if body_table ~= nil then
        body = cjson.encode(body_table)
        headers["Content-Type"] = "application/json"
        headers["Content-Length"] = tostring(#body)
      end

      local response_chunks = {}
      local ok, status_or_err = http.request({
        url = ZITADEL_API_BASE_URL .. path,
        method = method,
        headers = headers,
        source = body and ltn12.source.string(body) or nil,
        sink = ltn12.sink.table(response_chunks),
      })

      if ok == nil then
        -- luasocketはネットワーク層の失敗(timeout含む)時に第1戻り値nil、第2戻り値へ
        -- エラー文字列("timeout"等)を返す。HTTPステータスコードとは別系統のため
        -- ここで明確に分岐する(task3.3のフェイルモード要件)。
        return false, nil, tostring(status_or_err)
      end

      local response_body = table.concat(response_chunks)
      local parsed = cjson.decode(response_body)
      return true, status_or_err, parsed
    end

    function auth_passdb_lookup(req)
      local ok, status, parsed = zitadel_api("POST", "/v2/sessions", {
        checks = {
          user = { loginName = req.user },
          password = { password = req.password },
        },
      })

      if not ok then
        req:log_error("zitadel session api unreachable: " .. tostring(parsed))
        return dovecot.auth.PASSDB_RESULT_INTERNAL_FAILURE, "zitadel api unreachable"
      end

      if status == 201 then
        return dovecot.auth.PASSDB_RESULT_OK, "nopassword=y"
      elseif status == 400 then
        -- code=3 "Password is invalid" が実機で確認済みの応答(task7.1参照)
        return dovecot.auth.PASSDB_RESULT_PASSWORD_MISMATCH, "password mismatch"
      elseif status == 404 then
        -- code=5 "User could not be found"。存在しないユーザー名でも誤パスワードと
        -- 同一のPASSWORD_MISMATCHへ正規化し、Dovecotから見て区別不能にする
        -- (task7.1が指摘したユーザー列挙耐性の緩和策を踏襲)。
        return dovecot.auth.PASSDB_RESULT_PASSWORD_MISMATCH, "password mismatch"
      else
        req:log_error("zitadel session api unexpected status " .. tostring(status))
        return dovecot.auth.PASSDB_RESULT_INTERNAL_FAILURE, "unexpected status " .. tostring(status)
      end
    end

    function auth_userdb_lookup(req)
      local ok, status, parsed = zitadel_api(
        "POST",
        "/v2/users",
        { queries = { { loginNameQuery = { loginName = req.user } } } }
      )
      if not ok or status ~= 200 or parsed == nil or parsed.result == nil or #parsed.result == 0 then
        return dovecot.auth.USERDB_RESULT_USER_UNKNOWN, "no such user"
      end

      return dovecot.auth.USERDB_RESULT_OK, "uid=5000 gid=5000 mail=" .. req.user
    end
  # Dovecot ACL plugin の有効化 (mail_plugins への acl 追加):
  # DMS の dovecot.cf 相当 (下の dovecot.cf キー) は /etc/dovecot/local.conf として
  # !include conf.d/*.conf の後に読み込まれる。一方 conf.d/15-lda.conf や
  # conf.d/20-imap.conf は protocol lda/imap { mail_plugins = $mail_plugins } を
  # 既に明示宣言しており、Dovecotはその時点の(acl追加前の)グローバル値で確定させてしまうため、
  # local.conf側でグローバルmail_pluginsを変更してもIMAP/LDA向けには反映されない
  # (doveconf -nで実機確認済み、protocol imap側が空のまま)。
  # そのため、acl追加分のみ conf.d/*.conf より前に読まれるファイル名 (05-) で別途注入する。
  05-acl-plugin.conf: |
    mail_plugins = $mail_plugins acl
  # Dovecot 認証設定オーバーライド
  dovecot.cf: |
    auth_mechanisms = plain login oauthbearer xoauth2

    # OAUTHBEARER: Zitadel introspection endpoint でトークン検証 (ファイル参照)
    passdb {
      driver = oauth2
      mechanisms = oauthbearer xoauth2
      args = /etc/dovecot/dovecot-oauth2.conf.ext
    }

    plugin {
      acl = vfile
      # acl_shared_dict: Dovecot が共有メールボックスの ACL 情報をキャッシュするための辞書。
      # 設定しないと "Mailbox not found from dovecot-acl-list" エラーが発生し、
      # 共有メールボックスが LIST に表示されない (acl_lookup_dict not enabled)。
      # /var/mail/aramakisai.com/ は docker:docker (0777) 所有で書込可能。
      acl_shared_dict = file:/var/mail/aramakisai.com/.acl_shared_dict
    }

    # 既定の inbox (private) namespace は separator 未指定で Maildir 既定の '.' を使う。
    # 一方で下の shared namespace 群は separator = '/' を指定しているため、
    # Dovecot の "All list=yes namespaces must use the same separator" 制約に抵触し、
    # 共有メールボックスへのアクセス・doveadm mailbox 操作が一切できない (タスク3.2で発覚)。
    # inbox namespace の separator も '/' に揃えて解消する (既存個人メールボックスはネスト
    # フォルダを持たないフラット構成のため、IMAP 区切り文字変更の影響は無い)。
    namespace inbox {
      inbox = yes
      separator = /
    }

    # ML 7件分の共有メールボックスを静的に定義する (acl_shared_dict は不使用、件数固定のため)
    # INDEX は /var/indexes ではなく /var/mail/.indexes 配下に置く: /var はルート所有 (0755) で
    # dovecot 実効 uid 5000 が mkdir できず "Permission denied" で Shared 名前空間が一切開けない
    # (タスク4.1で発覚)。メール PVC のルート /var/mail は 0777 docker 所有で書込可能なため、
    # ここに共有 index を置く。DMS は全ユーザー単一 uid 5000 のため共有 index で問題ない。
    #
    # アクセス権は user-patches.sh が各部署ルートの dovecot-acl に `authenticated` として付与し、
    # Zitadel 認証済みの全ユーザーが全 ML を同等に読み書きできる (利用者は実行委員のみのため
    # 部署別の閲覧制限は設けない。mailing-list-shared-mailbox design.md 参照)。
    #
    # list = children は子メールボックスを持つ namespace のみ LIST に出すため、各部署 maildir に
    # 最低1つの子メールボックスが必要。
    # 受信専用で一度もクライアント接続していない部署は子が無く LIST に出ないため、
    # user-patches.sh が全部署に標準サブフォルダ (Drafts/Sent/Junk/Trash) を冪等作成する。
    # 部署受信トレイ (maildir ルート) は "Shared/<dept>" 自体を SELECT してアクセスする。
    # subscriptions = no は Roundcube 側 imap_force_list=true で補い、購読不要で表示させる。
    namespace {
      type = shared
      separator = /
      prefix = Shared/planning/
      location = maildir:/var/mail/aramakisai.com/planning:INDEX=/var/mail/.indexes/aramakisai.com/planning
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/accounting/
      location = maildir:/var/mail/aramakisai.com/accounting:INDEX=/var/mail/.indexes/aramakisai.com/accounting
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/booth/
      location = maildir:/var/mail/aramakisai.com/booth:INDEX=/var/mail/.indexes/aramakisai.com/booth
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/stage/
      location = maildir:/var/mail/aramakisai.com/stage:INDEX=/var/mail/.indexes/aramakisai.com/stage
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/pr/
      location = maildir:/var/mail/aramakisai.com/pr:INDEX=/var/mail/.indexes/aramakisai.com/pr
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/admin/
      location = maildir:/var/mail/aramakisai.com/admin:INDEX=/var/mail/.indexes/aramakisai.com/admin
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/general-affairs/
      location = maildir:/var/mail/aramakisai.com/general-affairs:INDEX=/var/mail/.indexes/aramakisai.com/general-affairs
      subscriptions = no
      list = children
    }
    namespace {
      type = shared
      separator = /
      prefix = Shared/noreply/
      location = maildir:/var/mail/aramakisai.com/noreply:INDEX=/var/mail/.indexes/aramakisai.com/noreply
      subscriptions = no
      list = children
    }
  # ML アドレス (配送専用)。DMS FILE provisioner が vmailbox と Dovecot userdb を生成し、
  # /var/mail/aramakisai.com/<localpart> へ配送する。パスワード欄は passwd-file passdb を
  # 置いていない (auth-passwdfile.inc) ため使われず、ログインには使えない。
  postfix-accounts.cf: |
    pr@aramakisai.com|{SHA512-CRYPT}!
    planning@aramakisai.com|{SHA512-CRYPT}!
    accounting@aramakisai.com|{SHA512-CRYPT}!
    booth@aramakisai.com|{SHA512-CRYPT}!
    stage@aramakisai.com|{SHA512-CRYPT}!
    admin@aramakisai.com|{SHA512-CRYPT}!
    general-affairs@aramakisai.com|{SHA512-CRYPT}!
    noreply@aramakisai.com|{SHA512-CRYPT}!
  # postmaster@ は RFC 5321 必須かつ DMARC/TLS-RPT レポート (terraform/dns.tf) と
  # DMS の起動通知の宛先。postmastar@/webmastar@ は既に外部に出回っている綴りのため残す。
  postfix-virtual.cf: |
    postmaster@aramakisai.com admin@aramakisai.com
    webmaster@aramakisai.com admin@aramakisai.com
    postmastar@aramakisai.com admin@aramakisai.com
    webmastar@aramakisai.com admin@aramakisai.com
    abuse@aramakisai.com admin@aramakisai.com
    administrator@aramakisai.com admin@aramakisai.com
    www@aramakisai.com admin@aramakisai.com
  # 送信者認可 (user-patches.sh で mua_sender_restrictions に組み込む):
  # - ML アドレス (エイリアス含む) を From にした送信は SASL 認証済みなら誰でも許可する。
  # - それ以外の From は sender-login-maps.cf の所有者と SASL ユーザーが一致する場合のみ
  #   許可 (noreply のみ)。未登録の From (個人アドレス等) は
  #   reject_authenticated_sender_login_mismatch で拒否される。
  ml-sender-access.cf: |
    pr@aramakisai.com permit_sasl_authenticated
    planning@aramakisai.com permit_sasl_authenticated
    accounting@aramakisai.com permit_sasl_authenticated
    booth@aramakisai.com permit_sasl_authenticated
    stage@aramakisai.com permit_sasl_authenticated
    admin@aramakisai.com permit_sasl_authenticated
    general-affairs@aramakisai.com permit_sasl_authenticated
    postmaster@aramakisai.com permit_sasl_authenticated
    webmaster@aramakisai.com permit_sasl_authenticated
    postmastar@aramakisai.com permit_sasl_authenticated
    webmastar@aramakisai.com permit_sasl_authenticated
    abuse@aramakisai.com permit_sasl_authenticated
    administrator@aramakisai.com permit_sasl_authenticated
    www@aramakisai.com permit_sasl_authenticated
  sender-login-maps.cf: |
    noreply@aramakisai.com noreply@aramakisai.com
  # DMS 公式の拡張フック (_setup 完了後に実行)。DMS の spoofing.sh が設定する
  # smtpd_sender_login_maps (FILE provisioner では「From と同名のログインなら許可」する
  # pcre を含み、個人アドレスでの送信を許してしまう) をここで上書きする。
  user-patches.sh: |
    #!/bin/bash
    # zitadel-auth.lua (Dovecot Lua Auth Bridge) が require("socket.http") /
    # require("cjson.safe") に依存するが、docker-mailserver イメージには
    # 同梱されていない (Debian bookworm、Lua 5.4)。
    apt-get update -qq
    apt-get install -y --no-install-recommends lua-socket lua-cjson
    postconf 'smtpd_sender_login_maps = texthash:/tmp/docker-mailserver/sender-login-maps.cf'
    # shellcheck disable=SC2016
    postconf 'mua_sender_restrictions = check_sender_access texthash:/tmp/docker-mailserver/ml-sender-access.cf, reject_authenticated_sender_login_mismatch, $dms_smtpd_sender_restrictions'
    # DMS の master.cf は submission/submissions で smtpd_reject_unlisted_recipient=no にしており、
    # 未登録の自ドメイン宛を受理して LMTP で 451 のまま deferred に滞留させる。
    postconf -P 'submission/inet/smtpd_reject_unlisted_recipient=yes' 'submissions/inet/smtpd_reject_unlisted_recipient=yes'
    # ML 共有メールボックスの ACL と標準サブフォルダ。Zitadel 認証済みの全ユーザーに同等権限を与える。
    # 'a'(ACL管理権)は付けない: 全員が同一権限のため、IMAP SETACL で他ユーザーのアクセスを
    # 外せてしまうだけで利点がない。サブフォルダは namespace の list = children が LIST 表示の
    # ゲートにするため必要 (子は dovecot-acl を持たず部署ルートの ACL を継承する)。
    for box in planning accounting booth stage pr admin general-affairs; do
      base="/var/mail/aramakisai.com/${box}"
      echo "authenticated lrwstipekx" > "${base}/dovecot-acl"
      chown 5000:5000 "${base}/dovecot-acl"
      for folder in Drafts Sent Junk Trash; do
        dir="${base}/.${folder}"
        [[ -d ${dir} ]] && continue
        mkdir -p "${dir}/cur" "${dir}/new" "${dir}/tmp"
        touch "${dir}/maildirfolder"
        chown -R 5000:5000 "${dir}"
        chmod -R 0700 "${dir}"
      done
    done
  # OpenDKIM KeyTable/SigningTable/TrustedHosts:
  # `setup config dkim domain` はこれらを /tmp/docker-mailserver/opendkim/ 配下に
  # 生成するが、その場所はConfigMap/Secretマウントではなく実行時の一時領域のため
  # Pod再作成のたびに失われる (dkim-keys Secret でマウントしているのは keys/ 配下の
  # 秘密鍵のみで、この3ファイルは対象外だった)。mail-tester.com でのDKIM未署名検知
  # (2026-07-03) で発覚。3ファイルとも秘匿情報を含まないためConfigMapで宣言的に永続化する。
  opendkim-keytable: |
    mail._domainkey.aramakisai.com aramakisai.com:mail:/etc/opendkim/keys/aramakisai.com/mail.private
  opendkim-signingtable: |
    *@aramakisai.com mail._domainkey.aramakisai.com
  opendkim-trustedhosts: |
    127.0.0.1
    localhost
    aramakisai.com
  # rspamd リソース削減:
  # メール流量がほぼ皆無(0.2 msg/sec)のため、rspamd_proxy worker の並列数を
  # 既定の2から1へ削減 (プロセスあたり約140MiBのメモリ削減)。
  # controller worker (管理UI/統計API、port 11334) も未使用のため無効化。
  # 副作用: Dovecot Sieveのlearn_spam/learn_ham (メールをJunkへ移動した際の
  # ベイズ学習フィードバック) は内部的にcontrollerのrspamc APIを叩くため、
  # 無効化に伴い学習フィードバックは効かなくなる (フィルタ自体は動作継続)。
  rspamd-worker-proxy.inc: |
    count = 1;
  rspamd-worker-controller.inc: |
    enabled = false;
  # mailserver は hostNetwork + NET_ADMIN のため、fail2ban の BAN はノード全体の
  # nftables input フックに入る。Pod IP が BAN されると SMTP だけでなくノード⇔その Pod
  # 間の全 TCP が破棄され、Zitadel Pod を BAN すると Lua Auth Bridge が到達不能になり
  # 認証失敗→再 BAN のループに陥る。クラスタ内 Pod (Pod CIDR) は BAN 対象外にする。
  fail2ban-jail.cf: |
    [DEFAULT]
    ignoreip = 127.0.0.1/8 10.42.0.0/16
