---
apiVersion: v1
kind: Service
metadata:
  name: roundcube
  namespace: prod
  labels:
    app: roundcube
spec:
  type: ClusterIP
  selector:
    app: roundcube
  ports:
    - name: http
      port: 80
      targetPort: 80
