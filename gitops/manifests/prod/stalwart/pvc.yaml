---
# Stalwart メールストレージ用 PVC
# メールデータは永続化が必須のため StatefulSet と組み合わせて使用
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stalwart-data
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi   # メールボックスの容量に応じて調整
