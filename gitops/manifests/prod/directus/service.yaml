---
apiVersion: v1
kind: Service
metadata:
  name: directus
  namespace: prod
  labels:
    app: directus
spec:
  type: ClusterIP
  selector:
    app: directus
  ports:
    - name: http
      port: 80
      targetPort: 8055
