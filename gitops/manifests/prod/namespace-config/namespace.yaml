---
# prod namespace 定義
# volsync.backube/privileged-movers: Mailserver (DMS) が root (UID 0) で動作するため
# VolSync mover も root で実行する必要がある
apiVersion: v1
kind: Namespace
metadata:
  name: prod
  annotations:
    volsync.backube/privileged-movers: "true"
