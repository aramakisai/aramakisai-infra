[Unit]
Description=Run os-update-notify daily after apt-daily-upgrade.timer

[Timer]
OnCalendar=*-*-* {{ "%02d" | format(os_update_notify_timer_hour | int) }}:{{ "%02d" | format(os_update_notify_timer_minute | int) }}:00
Persistent=true

[Install]
WantedBy=timers.target
