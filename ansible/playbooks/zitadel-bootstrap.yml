---
# Zitadel初回admin/PATブートストラップ (本番・k3d検証環境の両対応)。
# kubectl操作はローカルからAPI経由で行うため、対象ホストはlocalhost
# (-iで渡すinventoryはKUBECONFIG取得等では使わない、他コマンドとの実行手順統一のため)。
- name: Zitadel初回ブートストラップ
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - role: zitadel-bootstrap
