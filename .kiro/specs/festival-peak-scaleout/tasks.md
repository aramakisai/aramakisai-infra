# Implementation Plan

本計画は IaC リポジトリの性質上、コード変更に加えて適用と検証の作業を含む。タスク 1 から 4 は本番構成を変更せず、タスク 5 以降が本番に触れる。タスク 0 はノード構成に依存せず、他のタスクの進行と無関係に先行して適用できる。

---

- [ ] 0. 画像配信エンドポイントのエッジキャッシュ
  - ノード数に依存せず、本番構成への影響も小さい。ノード増設 (タスク 3 以降) を待たずに先行して適用できる
  - _Requirements: 9.9_

- [ ] 0.1 Cache Rule を `/api/media/serve/` に対して実効させるようコードを変更する
  - `terraform/cloudflare_media_cache.tf` の `cloudflare_ruleset.directus_assets_cache` を対象に、`/api/media/serve/` への 302 が `Cache-Control` や `Set-Cookie` の有無によらず確実にキャッシュされるよう `action_parameters` を調整する (`origin_cache_control` の明示、ルール一致順の見直し等)
  - `/api/media/file/*` (実ファイル本体) の既存のキャッシュ挙動を変更しない
  - 完了状態: 変更がコミットされ、対象を限定した plan で該当ルールの差分のみが示されること
  - _Requirements: 9.1, 9.2, 9.3_

- [ ] 0.2 適用しキャッシュが有効であることを検証する
  - 対象を限定して apply する
  - `/api/media/serve/{id}/{size}` への 2 回目以降のリクエストで `cf-cache-status: HIT` を確認する
  - キャッシュされた 302 をブラウザで開き、画像が正常に表示されることを確認する
  - 構内マップで同一区画を複数回タップし、CMS 側のリクエスト数が画像枚数に比例して増えないことを確認する
  - `Location` が署名付き URL でないことを踏まえた TTL であることを記録する。将来署名付き URL に変わった場合は TTL をその有効期限未満に短縮する
  - Cloudflare のキャッシュパージによる無効化手段を確認し記録する
  - 完了状態: HIT・画像の正常表示・リクエスト増幅の抑制の 3 点が確認され記録されていること
  - _Requirements: 9.4, 9.5, 9.6, 9.7, 9.8_
  - _Depends: 0.1_

---

- [x] 1. ベースライン測定の基盤整備と実測

- [x] 1.1 ブレークポイント探索用の負荷シナリオを用意する
  - CMS の API を対象とし、段階的にレートを上げ続ける構成とする。plateau と ramp-down を設けない
  - 応答が遅延してもレートが落ちない executor を用いる
  - エラー率の閾値超過で自動停止する設定を入れる。本番へ印加するため、壊れた後も負荷を掛け続けない
  - 対象ベース URL を切り替えられるようにし、Cloudflare を経由する経路と経由しない経路の双方で同一シナリオを使えるようにする
  - 完了状態: シナリオを短時間・低レートで試走させ、リクエストが送信されメトリクスが出力されること
  - _Requirements: 1.1, 1.6, 1.7_

- [x] 1.2 オリジン直経路でブレークポイントを実測する
  - Cloudflare を経由しない経路から負荷を印加する
  - HTTP 5xx またはタイムアウトが発生し始めたレートを記録する
  - 実行中のノードのメモリ使用量・CPU 使用率・Pod の OOMKilled 発生有無・CMS と cms-db のレスポンス劣化を記録する
  - 実行前に CNPG バックアップの正常性を確認し、利用の少ない時間帯に実施する
  - 測定値が実行元の回線帯域に律速していないことを確認する
  - 完了状態: ブレークポイントに到達したレートと、その時点のエラー種別・リソース使用状況が記録されていること
  - _Requirements: 1.2, 1.3, 1.4, 1.6_

- [x] 1.3 Cloudflare 経由の経路を測定しエッジキャッシュの効果を確認する
  - 短時間かつ小規模に留める。レート制限や WAF の判定に触れることを避ける
  - オリジン直の結果と区別して記録する
  - 完了状態: 両経路の結果が別々に記録され、エッジキャッシュがオリジン負荷をどの程度吸収しているかを説明できること
  - _Requirements: 1.2_

- [x] 1.4 合格ラインを算出しステートレス分散の要否を判定する
  - 公開サイトを 1 ページ閲覧した際に CMS API へ発生するリクエスト数を実測する。Workers のキャッシュ設定に依存して大きく変動するため推定しない
  - 来場者数と 1 人あたり平均ページビュー数を実績値から取得する
  - 設計が定義する算出式と係数により合格ラインを求める
  - ブレークポイントが合格ラインを上回る場合はステートレス分散を見送る判断を記録する
  - この判定がデータ層の冗長化には影響しないことを記録に明記する
  - 算出に用いた実績値を公開リポジトリ配下の文書へ記載しない
  - 完了状態: 合格ラインの数値と判定結果が算出過程とともに記録され、タスク 5.8 の実施可否が確定していること
  - _Requirements: 1.5, 1.8, 1.9_

---

- [x] 2. インフラコードの準備

本番構成を変更しない。コードのマージのみを行う。

- [x] 2.1 ノード定義をノードごとのインスタンスタイプ指定に対応させる
  - ノード定義の各エントリがインスタンスタイプを保持する構造へ変更する
  - 既存ノードには現行値を明示し、実体との差分を生じさせない
  - 追加ノード 2 台のエントリを定義する。プライベート IP は既存サブネットの未使用アドレスから割り当てる
  - 完了状態: 対象を限定した plan で追加ノード 2 台のみが新規作成として示され、既存ノードに差分が出ないこと
  - _Requirements: 2.1, 2.3, 2.4, 2.7_

- [x] 2.2 spread 型の placement group を定義し全ノードの所属先とする
  - spread 型の placement group リソースを新規に定義する
  - 全ノードがこれに所属するようノード定義から参照する
  - 既存ノードの所属変更が再作成ではなく in-place の更新として示されることを plan で確認する
  - 完了状態: plan において既存ノードの差分が所属先の変更のみであり、再作成が示されないこと
  - _Requirements: 2.2, 2.3, 2.4_
  - _Depends: 2.1_

- [x] 2.3 (P) 追加ノードを server グループとして登録する
  - 追加ノードを既存 playbook の該当グループとして inventory に定義する
  - 既存ノードが属するグループの内容を変更しない。join 先の解決がこのグループの先頭要素に依存するため
  - 対象を限定した check 実行により、既存ノードに変更が生じないことを確認する
  - 完了状態: check 実行が追加ノードのみを対象とし、既存ノードに対して変更なしと報告すること
  - _Requirements: 2.5, 2.6, 2.7_
  - _Boundary: inventory 定義_

- [x] 2.4 (P) API 証明書の代替名を全 server ノードへ適用する
  - 代替名の設定をクラスタ初期化の分岐外へ移し、ノード種別によらず適用されるようにする
  - 設定値は各ノード自身の接続名とプライベート IP とし、他ノードの値を含めない
  - 完了状態: テンプレートの描画結果において、初期化ノードと追加ノードの双方に代替名が含まれること
  - _Requirements: 2.7, 3.5_
  - _Boundary: tls-san 適用_

- [x] 2.5 (P) 障害検知の自動復旧を Git 管理下で停止できるようにする
  - 発火を停止するフラグまたは条件分岐をワークフローへ追加する
  - 有効・無効の状態が Git 履歴から追跡できる形にする。管理画面の操作のみで完結する手段は用いない
  - 現行の判定対象が単一ノード決め打ちであり、クォーラムの健全性を考慮しないことを踏まえ、停止を既定の対処とする
  - 完了状態: フラグの切り替えがコミットとして残り、停止状態でワークフローが発火しないこと
  - _Requirements: 6.1, 6.2, 6.5_
  - _Boundary: DR 制御_

- [x] 2.6 対象を限定した差分確認の手順を確定する
  - 既知の到達不能なプロバイダにより、対象を限定しない差分確認が完了しないことを前提とする
  - 追加ノードを指すリソースアドレスを対象として指定する。依存リソースは自動的に含まれる
  - 限定範囲外の差分が確認できない事実を作業記録に残す
  - 完了状態: 限定した差分確認が完走し、追加ノードの新規作成と既存ノードの所属先変更のみが示されること
  - _Requirements: 2.8, 2.9_
  - _Depends: 2.1, 2.2_

---

- [ ] 3. 検証環境でのノード増減手順の確立

- [ ] 3.1 使い捨ての検証クラスタを構築する
  - 本番と同一の構成管理ロールを用いて構築する
  - 既存のローカル検証資産を流用し、新規に環境を作り込まない
  - 検証対象はメンバーの増減に限る。アプリケーションのデプロイは行わない
  - 最初の工程として、この環境が埋め込みデータストアのメンバー増減を本番同等に再現するかを確認する
  - 完了状態: 単一ノードのクラスタが起動し、API が応答すること。再現性に問題がある場合はその旨が記録されていること
  - _Requirements: 3.1, 3.2_

- [ ] 3.2 拡張方向の手順を確立する
  - 1 ノードから 3 ノードへ段階的に拡張し、各段階でメンバー数とノードの状態を確認する
  - メンバーが 2 となる区間を通ることを確認し、この区間の耐障害性が単一ノード構成より低いことを記録する
  - 代替名の設定変更を既存ノードへ適用し、証明書の再生成またはサービス再起動が発生するかを記録する
  - 完了状態: 3 ノード構成が成立し、各段階の所要時間と観測結果が記録されていること
  - _Requirements: 3.3, 3.4, 3.5_

- [ ] 3.3 データベースクラスタの増減と主系の移動を検証する
  - インスタンス数を 1 から 3 へ増加させ、各インスタンスが別ノードへ配置されることを確認する
  - インスタンス数を 3 から 1 へ減少させる手順を確認する
  - 主系が縮退後に残すノード以外にある場合の移動手順を確認する
  - 主系の障害時に待機系が昇格する挙動を確認する。同期設定の要否をここで判断する
  - 完了状態: 増減双方の手順が動作し、主系の移動と昇格の所要時間が記録されていること
  - _Requirements: 3.6, 3.7_

- [ ] 3.4 縮退方向の手順を確立する
  - 作業開始前にデータストアの複製を取得し、ノード外へ退避する工程を含める
  - ノードを 1 台ずつ削除し、各段階でメンバー数とクォーラムの状態を確認する
  - 3 台から 2 台への縮退と、2 台から 1 台への縮退を区別して観測する。後者でクォーラムが再構成される
  - クォーラム喪失またはクラスタ停止が発生した場合は原因を特定し、手順を修正して再検証する
  - 完了状態: 1 ノード構成へ復帰し、API が応答してワークロードがスケジュール可能であること
  - _Requirements: 3.8, 3.9, 3.10, 3.12_

- [ ] 3.5 接続名の後始末を含む固有工程を実環境の最小構成で確認する
  - 削除後も接続デバイスが残存し、次回作成時に別名で登録される挙動を確認する
  - 削除手順が既存の復旧スクリプトの実装と一致することを確認する
  - プライベート IP の割り当てと初期化処理の動作を確認する
  - 完了状態: デバイス削除を含む一連の手順が実環境で通り、次回作成時に想定どおりの名前で登録されること
  - _Requirements: 3.11_

- [ ] 3.6 増減手順を文書化する
  - 拡張方向と縮退方向の双方を記載する
  - 各工程をどの検証環境で確認したかを明記する。どちらでも確認していない領域を残さない
  - 完了状態: 本番作業時に参照できる手順書が存在し、検証済み範囲が読み取れること
  - _Requirements: 3.13_

- [ ] 3.7 検証環境を解放する
  - 検証クラスタのリソースを削除する
  - 実環境を使用した場合は接続デバイスも併せて削除する
  - 完了状態: 検証用のリソースと接続デバイスが残っていないこと
  - _Requirements: 3.14_

---

- [x] 4. データ層冗長化のマニフェスト準備

稼働中のデータベースクラスタのみを対象とする。停止中のワークロードに属するものは変更しない。

- [x] 4.1 (P) CMS のデータベースクラスタを冗長構成に変更する
  - インスタンス数を 3 へ変更する
  - インスタンスを別ノードへ配置する制約を加える
  - 各ノードのディスクが増加分を収容できることを事前に確認する
  - 完了状態: マニフェストがコミットされ、3 ノード構成の適用時に各インスタンスが別ノードへ配置される状態になっていること
  - _Requirements: 7.1, 7.3, 7.5, 7.13_
  - _Boundary: データ層冗長化_

- [x] 4.2 (P) 認証基盤のデータベースクラスタを冗長構成に変更する
  - インスタンス数を 3 へ変更し、別ノード配置の制約を加える
  - この基盤が停止すると管理画面と GitOps への操作が行えなくなるため、来場者経路に乗らないが対象に含める
  - 完了状態: マニフェストがコミットされ、適用時に各インスタンスが別ノードへ配置される状態になっていること
  - _Requirements: 7.1, 7.3, 7.5, 7.13_
  - _Boundary: データ層冗長化_

- [x] 4.3 対象外のワークロードを据え置く判断を記録する
  - 停止中のワークロードに属するデータベースクラスタを変更しない。稼働していないため冗長化の効果がなく、ディスクを消費するのみである
  - メールサーバーの配置制約を変更しない。特定ポートの bind と逆引き設定により移動できない
  - 外部公開経路の構成を変更しない。来場者トラフィックを担わないため移動しても効果がない
  - 停止中のワークロードを再開しない
  - 完了状態: 対象外リストが記録され、以降の作業でこれらに触れない根拠が参照できること
  - _Requirements: 7.2, 7.9, 7.10, 7.11_

---

- [ ] 5. 本番への投入と冗長化
  - 3 ノード構成が存在する唯一の期間。開催日より前に完了し、動作確認の猶予を確保する
  - _Requirements: 4.1_

- [ ] 5.1 作業前の復旧手段を確保する
  - データストアの複製を取得し、ノード外へ退避する。既定ではノードのローカルにのみ保存されるため、ノード喪失時に同時に失われる
  - データベースのバックアップが正常であることを確認する
  - 退避先から読み出せることを確認する
  - 完了状態: 退避済みの複製が存在し、読み出せることが確認されていること
  - _Requirements: 4.11_

- [ ] 5.2 自動復旧を停止する
  - タスク 2.5 で用意したフラグにより発火を停止し、コミットとして記録する
  - 無効化の期間と理由、復帰予定日を記録に含める
  - 適用作業より前に完了させる。ノード作成中の過渡状態で検知機構が発火することを防ぐ
  - 完了状態: フラグが停止状態でコミットされ、定期実行が発火しないこと
  - _Requirements: 6.2, 6.3, 6.5_
  - _Depends: 2.5_

- [ ] 5.3 追加ノードを作成する
  - 対象を限定した差分確認を行い、追加ノードの新規作成のみが示されることを確認する
  - 作成時に配置グループを指定する。新規作成時の指定はサーバー停止を伴わない
  - 対象を限定して適用する
  - 完了状態: 追加ノード 2 台が作成され、互いに異なる物理ホストへ配置されていること
  - _Requirements: 4.2, 2.8, 2.9_
  - _Depends: 2.6_

- [ ] 5.4 追加ノードをクラスタへ参加させる
  - 対象を限定して構成管理を実行し、追加ノードを server として参加させる
  - 既存ノードに変更が及ばないことを確認する
  - 拡張の過程でメンバーが 2 となる区間を短く保つ
  - 完了状態: メンバー数 3 かつ全ノードが正常な状態であること
  - _Requirements: 4.3, 2.6_
  - _Depends: 3.6_

- [ ] 5.5 データ層を冗長化する
  - タスク 4.1 と 4.2 のマニフェストを適用する
  - 各インスタンスが別ノードへ配置されたことを確認する
  - 完了状態: 対象クラスタの待機系が別ノードで正常に稼働していること
  - _Requirements: 7.1, 7.3, 7.4_
  - _Depends: 4.1, 4.2_

- [ ] 5.6 既存ノードを配置グループへ追加する
  - 既存サーバーの追加はオフラインであることが前提となるため、ノードを停止して実施する
  - データ層の冗長化完了後に行う。停止中もメンバー 2 でクォーラムが維持され、データベース接続は待機系が引き継ぐ
  - メールサーバーがこの区間で停止することを認識し、停止時間を最小に保つ
  - 追加に失敗した場合はノードを起動して従前の状態へ戻し、追加ノードのみが所属する状態を記録する
  - 完了状態: 全ノードが同一の配置グループに所属し、再起動後に正常な状態へ復帰していること
  - _Requirements: 4.4, 4.5, 4.6, 4.7_
  - _Depends: 5.5_

- [ ] 5.7 投入後の全サービスを確認する
  - GitOps・CMS・認証基盤・メールサーバーが正常応答することを確認する
  - メールサーバーが既存ノード上で動作し続けていることを確認する
  - 意図しないワークロードが追加ノードへ移動していないかを確認する。配置制約を設けていないため、再スケジュールにより移動しうる
  - 既存サービスへ影響が発生した場合はタスク 6 の手順で 1 ノード構成へ復帰する
  - 完了状態: 全サービスが正常応答し、想定外の配置変化がないことが確認されていること
  - _Requirements: 4.8, 4.9, 4.10_

- [ ] 5.8 ステートレスワークロードを分散する
  - タスク 1.4 の判定が必要性を示した場合に限り実施する
  - CMS のレプリカ数を増加させ、複数ノードへ分散配置する制約を加える
  - 変更を Git のコミットとして行い、クラスタへの直接操作を行わない
  - 完了状態: レプリカが複数ノードへ分散配置され、変更前の値が記録に残っていること
  - _Requirements: 7.6, 7.7, 7.13_
  - _Depends: 1.4_

- [ ] 5.9 冗長化後の性能を再測定する
  - タスク 1.1 のシナリオを再実行する
  - ステートレス分散を実施した場合、ブレークポイントが変更前を上回ることを確認する
  - 完了状態: 3 ノード構成でのブレークポイントが記録され、変更前との比較ができること
  - _Requirements: 7.8_

---

- [ ] 6. 縮退と原状復帰

タスク 3.6 で文書化した手順に従う。検証を経ていない操作を含めない。

- [ ] 6.1 縮退前の復旧手段を確保する
  - データストアの複製を取得し、ノード外へ退避する
  - 完了状態: 退避済みの複製が存在し、読み出せることが確認されていること
  - _Requirements: 5.1, 5.8_

- [ ] 6.2 ワークロードの配置を変更前へ戻す
  - レプリカ数と配置制約を変更前の値へ戻す
  - 削除対象ノード上で稼働するものを事前に減らす
  - 完了状態: 対象ワークロードが変更前の構成で稼働していること
  - _Requirements: 7.12, 7.13_

- [ ] 6.3 データ層を単一構成へ戻す
  - 各データベースクラスタの主系の所在を確認し、残すノード以外にある場合は移動する
  - インスタンス数を 1 へ戻し、待機系の削除完了を待つ
  - 主系が削除対象ノード上にあるまま進めると、昇格先のない状態でデータを保持するインスタンスが失われる
  - 完了状態: 各クラスタが単一インスタンスとなり、残すノード上で稼働していること
  - _Requirements: 5.2, 5.3_
  - _Depends: 6.2_

- [ ] 6.4 ノードを段階的に削除する
  - 1 台ずつ削除し、各段階でメンバー数とクォーラムの状態を確認する
  - 2 台から 1 台への段階でクォーラムが再構成される。この段階を最も慎重に扱う
  - クラスタが停止した場合は退避済みの複製から復旧する
  - 完了状態: 単一ノードで API が応答し、ワークロードがスケジュール可能であること
  - _Requirements: 5.4, 5.8_
  - _Depends: 6.3_

- [ ] 6.5 サーバーと接続デバイスを削除する
  - 対象を限定して適用し、追加サーバーを削除する
  - 削除後に課金が停止していることを確認する
  - 対応する接続デバイスを削除する。残存すると次回作成時に別名で登録され、構成管理が接続できなくなる
  - 完了状態: 追加サーバーと接続デバイスが存在せず、構成が移行前と等価な状態を宣言していること
  - _Requirements: 5.5, 5.6, 5.7_

- [ ] 6.6 自動復旧を再有効化する
  - タスク 5.2 で停止したフラグを戻し、コミットとして記録する
  - 定期実行が通常どおり動作することを確認する
  - 完了状態: フラグが有効状態でコミットされ、検知機構が動作していること
  - _Requirements: 6.4, 6.5_
  - _Depends: 6.5_

---

- [ ] 7. 構成記述の同期

- [x] 7.1 (P) プロジェクトメモリのノード構成に関する記述を実態に合わせる
  - ノード構成の記述を現行構成と一致させる
  - 参照先の仕様の状態に関する記述を実態と一致させる
  - 存在しない構成管理ロールへの言及を修正する
  - 実測値を含めず、手順と構成のみを記載する
  - 完了状態: 記述が現行構成と一致し、存在しない対象への参照が残っていないこと
  - _Requirements: 8.1, 8.2, 8.3, 8.5_
  - _Boundary: ドキュメント同期_

- [ ] 7.2 (P) 確立した増減手順を運用ドキュメントへ反映する
  - タスク 3.6 の手順書を運用ドキュメントの体系へ取り込む
  - 実測値を含めず、手順と構成のみを記載する
  - 完了状態: 運用ドキュメントから増減手順が参照でき、検証済み範囲が読み取れること
  - _Requirements: 8.4, 8.5_
  - _Boundary: ドキュメント同期_
  - _Depends: 3.6_
